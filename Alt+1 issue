Go to `global shortcuts` in setting (in KDE). Then go to `Konversation` tap and define a custom shortcut, the remove it again.

Now the problem is solved and you can easily change between tabs (in editor, browser and etc.).
